# Images

Drop your dashboard deliverables here.

>>> CHRISTOPHER: After you build the dashboard from `../BUILD_GUIDE.md`, add:
- **Screenshot(s)** of your finished single-page dashboard (PNG), e.g.
  `dashboard.png`. Embed at least one in the project `../README.md` Findings
  section like: `![Dashboard](images/dashboard.png)`.
- The **`.pbix` file** itself (e.g. `healthcare-quality.pbix`) so the project is
  fully reproducible and recruiters can open it.

Tip: a clean full-page screenshot (Windows: `Win + Shift + S`) at a readable
resolution works best for the README preview.
